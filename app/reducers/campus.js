import axios from 'axios'

const initialState = {};

const campusReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default campusReducer;
